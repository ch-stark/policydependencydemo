apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-app-secret
  namespace: dep-demo
  labels:
    demo.example.com/part-of: policy-dependency-demo
    demo.example.com/stage: app-secret
data:
  stage: app-secret
  message: Second template. extraDependencies wait for demo-app-config ConfigurationPolicy Compliant.
